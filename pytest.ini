[pytest]
markers =
    hardware: requires a real audio input device (skipped in CI / headless).
    bundle: requires a built Transcriber.app bundle (run make_app.sh first).
    summarization: requires the real local summarization GGUF model (skipped in CI / headless).
asyncio_mode = strict
